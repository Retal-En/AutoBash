<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">


        <!-- Sequences for Contract -->
        <record id="seq_contract" model="ir.sequence">
            <field name="name">Sales Contract</field>
            <field name="code">sale.contract</field>
            <field name="prefix">Car/Contract/</field>
            <field name="padding">6</field>
            <!-- <field name="domain_force">[('company_id.id','=',company_id)]</field> -->
            <!-- <field name="company_id" eval="False"/> -->
        </record>


        <!-- Sequences for Sales Approval -->
        <record id="seq_sale_approval" model="ir.sequence">
            <field name="name">Sales Order Approval</field>
            <field name="code">sale.order.approval</field>
            <field name="prefix">AP</field>
            <field name="padding">6</field>
            <!-- <field name="domain_force">[('company_id.id','=',company_id)]</field> -->
            <!-- <field name="company_id" eval="False"/> -->
        </record>

        <!-- Sequences for PDC OUT -->
      <!--   <record id="seq_pdc_out" model="ir.sequence">
            <field name="name">PDC Payables</field>
            <field name="code">pdc.payment.out</field>
            <field name="prefix">PDC/OUT/</field>
            <field name="padding">8</field>
        </record> -->
       
    </data>
</odoo>
